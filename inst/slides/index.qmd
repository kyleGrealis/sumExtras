---
title: "sumExtras: Streamlining Table Workflows in Clinical Research with R"
author:
  - name: "Kyle Grealis"
    affiliation: "University of Miami"
date: "May 8, 2026"
date-format: "MMM D, YYYY"
---

```{r}
#| echo: false
suppressPackageStartupMessages({
  library(tidyverse)
  library(sumExtras)
  library(gtsummary)
  library(gt)
})

options(
  sumExtras.auto_labels = FALSE,
  sumExtras.prefer_dictionary = FALSE,
  digits = 4
)
```

# Introduction
 
> **sumExtras**: "gt**sum**mary table **Extras**"


## The plan
::: {.incremental}
+ Apply a JAMA-ready theme
+ Customize missing values with `clean_table()`
+ One-line table formatting with `extras()`
+ Auto-label variables with `add_auto_labels()`
  <!-- + Create a variable dictionary
  + Set package options -->
+ Add variable group headers [with _**style**_ and \
 [**background coloring**]{.coloring}]{ .fragment }
<!-- + Style and color group rows -->
+ Put it all together in one pipeline
:::


## What we'll build

![](img/final_table.png){fig-align="center"}


## Creating a summary table

Results from a simulated study of two chemotherapy agents.

A dataset containing the baseline characteristics of 200 patients who received Drug A or Drug B. Dataset also contains the outcome of tumor response to the treatment.

```{r}
#| echo: true
#| eval: true
gtsummary::trial
```

##

`mutate` the data to simulate missing values

```{r}
#| echo: true
#| eval: true
#| code-line-numbers: "|17"
dat <- trial |>
  mutate(
    marker = case_when(
      trt == "Drug B" ~ 0,
      .default = marker
    ),
    grade = case_when(
      trt == "Drug B" ~ NA,
      .default = grade
    )
  )
head(dat, 5)
```

## Initial `gtsummary` table

<br>

```{r}
#| output-location: column
dat |>
  tbl_summary(by = trt)
```


## Setting a theme

<br>
<!-- Use `sumExtras::use_jama_theme()` for a more compact table. -->

```{r}
#| output-location: column
sumExtras::use_jama_theme()

dat |>
  tbl_summary(by = trt)
```

::: {.fragment}
`use_jama_theme()` is a wrapper function for \
 `gtsummary::theme_gtsummary_compact("jama")`
:::


## Cleaning the table

![](img/tbl_with_zeros.png){width="45%"}

::: {.fragment}
**THE GOAL:**
Draw attention to complete, non-`NA` values with `clean_table()`.
Replace `0 (0.00, 0.00)`, `0 (NA%)`, and similar missingness with an emdash (`—`).
:::

## Cleaning the table

```{r}
#| eval: false
dat |>
  tbl_summary(by = trt) |>
  clean_table()
```

:::: {.columns}
::: {.column}
Without `clean_table()`
```{r}
#| echo: false
use_jama_theme()
dat |>
  tbl_summary(by = trt)
```
:::

::: {.column}
With `clean_table()`
```{r}
#| echo: false
use_jama_theme()
dat |>
  tbl_summary(by = trt) |>
  clean_table()
```
:::
::::


## Adding labels, *p*-values, and other goodies

```{r}
#| echo: false
#| eval: false
dat |>
  tbl_summary(
    by = trt,
    label = list(
      age ~ "Age at Enrollment (years)",
      marker ~ "Marker Level (ng/mL)",
      stage ~ "T Stage",
      grade ~ "Tumor Grade",
      response ~ "Tumor Response",
      death ~ "Patient Died"
    )
  ) |>
  modify_header(label = "") |>
  bold_labels() |>
  add_p() |>
  bold_p() |>
  add_overall() |>
  clean_table()
```

<!-- ::: {.incremental .smallest}
* Removed "Characteristic" from variable heading
* Added *p*-values
* Bold labels & significant *p*-values
* Added "Overall" column
* Applied `clean_table()`
::: -->

![](img/table-with-goodies-1.png){width=85%}  

This is more like what I want...

## Adding labels, *p*-values, and other goodies
![](img/table-with-goodies-2.png){width=85%}  

Removed "Characteristic" from variable heading

## Adding labels, *p*-values, and other goodies
![](img/table-with-goodies-3.png){width=85%}  

Added *p*-values

## Adding labels, *p*-values, and other goodies
![](img/table-with-goodies-4.png){width=85%}  

Bold labels & significant *p*-values

## Adding labels, *p*-values, and other goodies
![](img/table-with-goodies-5.png){width=85%}  

Added "Overall" column

## Adding labels, *p*-values, and other goodies
![](img/table-with-goodies-6.png){width=85%}  

Applied `clean_table()`


## Adding labels, *p*-values, and other goodies

:::: {.columns}
::: {.column}
```{r}
#| eval: false
dat |>
  tbl_summary(
    by = trt,
    label = list(
      age ~ "Age at Enrollment (years)",
      marker ~ "Marker Level (ng/mL)",
      stage ~ "T Stage",
      grade ~ "Tumor Grade",
      response ~ "Tumor Response",
      death ~ "Patient Died"
    )
  ) |>
  modify_header(label = "") |>
  bold_labels() |>
  add_p() |>
  bold_p() |>
  add_overall() |>
  clean_table()
```
:::

::: {.column}
```{r}
#| eval: false
dat |>
  tbl_summary(
    by = trt,
    label = list(
      age ~ "Age at Enrollment (years)",
      marker ~ "Marker Level (ng/mL)",
      stage ~ "T Stage",
      grade ~ "Tumor Grade",
      response ~ "Tumor Response",
      death ~ "Patient Died"
    )
  ) |>
  extras()
```
:::{.fragment}
Six lines converted to one!
:::

:::

::::


# Labelling hierarchy

::: {.incremental}
* Dataset label attributes
* Using `gtsummary::tbl_*`'s `label` parameter
* Using `sumExtras` auto-labelling
:::

## Default table without labels {.col-45-55}

`gtsummary` reads attribute labels natively!

```{r}
#| eval: true
#| output-location: column
# Set an explicit attribute label:
attr(dat$age, "label") <-
  "Age ATTRIBUTE"

dat |>
  tbl_summary(by = trt) |>
  extras()
```

::: {.fragment .smallest}
`dat` was created from `trial`. `trial` had attributes, but using `mutate`
earlier removed attributes from `dat$marker` and `dat$grade`.
:::


## The `gtsummary::tbl_*` labeling method

```{r}
#| eval: true
#| output-location: column
#| code-line-numbers: "|1-2|7-10"
attr(dat$marker, "label") <-
  "Marker ATTRIBUTE"

dat |>
  tbl_summary(
    by = trt,
    label = list(
      marker ~ "Marker LABEL",
      stage ~ "Stage LABEL"
    )
  ) |>
  extras()
```

Labels set in `tbl_*`'s `label` parameter **always** take priority!

::: {.fragment .smallest}
`dat$grade` still has no label because it doesn't have a label attribute
*nor* was any label assigned within the `tbl_summary()` `label` parameter.
:::


## Creating a Variable Dictionary

A dictionary object is a data frame with two columns:  
* `variable`  
* `description`

<!-- Show the creation of the data dictionary object
Highlight lines, then show the created object incrementally to the side -->

:::: {.columns}
::: {.column width="55%"}
```{r}
#| echo: true
#| code-line-numbers: "|2"
dictionary <- tibble::tribble(
  ~variable,    ~description,
  "trt",        "Chemotherapy Treatment",
  "age",        "Age DICT",
  "marker",     "Marker DICT",
  "stage",      "Stage DICT",
  "grade",      "Grade DICT",
  "response",   "Response DICT",
  "death",      "Patient Died DICT"
)
```
:::
::: {.column width="45%"}
```{r}
dictionary
```
:::
::::

::: {.incremental}
::: {.callout-note .fragment}
The column names are case-*in*sensitive. `variable`, `Variable`, and `VARIABLE`
will work all the same.
:::
:::


## Use the `dictionary` with `add_auto_labels()` {.col-45-55}

```{r}
#| output-location: column
dat |>
  tbl_summary(
    by = trt,
    label = list(
      age ~ "Age LABEL",
      marker ~ "Marker LABEL",
      stage ~ "T Stage LABEL",
      grade ~ "Grade LABEL",
      response ~ "Response LABEL",
      death ~ "Patient Died LABEL"
    )
  ) |>
  extras() |>
  add_auto_labels()
```

**Again**: Labels set in `tbl_*`'s `label` parameter **always** take priority!



## Use the `dictionary` with `add_auto_labels()` (part 2) {.col-45-55}

```{r}
#| code-line-numbers: "|4-11"
#| output-location: column
dat |>
  tbl_summary(
    by = trt,
    # label = list(
    #   age ~ "Age FROM LABEL (years)",
    #   marker ~ "Marker Level (ng/mL)",
    #   stage ~ "T Stage",
    #   grade ~ "Tumor Grade",
    #   response ~ "Tumor Response",
    #   death ~ "Patient Died"
    # )
  ) |>
  extras() |>
  add_auto_labels()
```

::: {.fragment}
**Remember** the hierarchy: Attributes > `label` parameter > `dictionary`
:::


## Use the `dictionary` with `add_auto_labels()` (part 3)

<br>
Set session (or persistent options):

```{r}
#| echo: true
#| eval: true
options(
  sumExtras.auto_labels = TRUE,
  sumExtras.prefer_dictionary = TRUE
)
```

**Reset** the hierarchy: `dictionary` takes precedence!

::: {.incremental}
* No longer need to use the `add_auto_labels()` function
  * `extras()` will now add the labels!
* The `dictionary` object will be found & used
:::

## Using `dictionary` and `options()`


:::: {.columns}
::: {.column width="45%"}
```{r}
#| eval: false
dat |>
  tbl_summary(by = trt) |>
  extras()
```

:::

::: {.column width="55%"}

```{r}
#| echo: false
dat |>
  tbl_summary(by = trt) |>
  extras()
```
:::
::::


# Grouping variables & adding color

```{r}
#| echo: false
dictionary <- tibble::tribble(
  ~variable,    ~description,
  "trt",        "Chemotherapy Treatment",
  "age",        "Age (years)",
  "marker",     "Marker Level (ng/mL)",
  "stage",      "T Stage",
  "grade",      "Tumor Grade",
  "response",   "Tumor Response",
  "death",      "Patient Died"
)
```

## Group variables

:::: {.columns}
::: {.column width="45%"}
```{r}
#| eval: false
dat |>
  tbl_summary(by = trt) |>
  extras() |>
  add_variable_group_header(
    header = "Demographics",
    variables = age:stage
  ) |>
  add_variable_group_header(
    header = "Clinical Measures",
    variables = grade:ttdeath
  ) |>
  add_group_styling()
```
::: {.fragment}
`add_group_styling()` uses **bold** & *italics* by default and restores
left-justified variable indentation.
:::

:::

::: {.column width="55%"}

```{r}
#| echo: false
dat |>
  tbl_summary(by = trt) |>
  extras() |>
  add_variable_group_header(
    header = "Demographics",
    variables = age:stage
  ) |>
  add_variable_group_header(
    header = "Clinical Measures",
    variables = grade:ttdeath
  ) |>
  add_group_styling()
```
:::
::::


## Add grouping colors

:::: {.columns}
::: {.column width="45%"}
```{r}
#| eval: false
dat |>
  tbl_summary(by = trt) |>
  extras() |>
  add_variable_group_header(
    header = "Demographics",
    variables = age:stage
  ) |>
  add_variable_group_header(
    header = "Clinical Measures",
    variables = grade:ttdeath
  ) |>
  add_group_styling() |>
  add_group_colors(
    color = c("#E1EAE2", "#FFF9E6")
  )
```

::: {.fragment}
**NOTE:** `add_group_colors()` CONVERTS the table to a `gt` table to apply the tweaks! Use this function as the *last* step in the pipeline.
:::

:::

::: {.column width="55%"}

```{r}
#| echo: false
dat |>
  tbl_summary(by = trt) |>
  extras() |>
  add_variable_group_header(
    header = "Demographics",
    variables = age:stage
  ) |>
  add_variable_group_header(
    header = "Clinical Measures",
    variables = grade:ttdeath
  ) |>
  add_group_styling() |>
  add_group_colors(color = c("#E1EAE2", "#FFF9E6"))
```
:::
::::


## Reduced code!

:::: {.columns}
::: {.column}
```{r}
#| eval: false
dat |>
  tbl_summary(
    by = trt,
    label = list(
      age ~ "Age (years)",
      marker ~ "Marker Level (ng/mL)",
      stage ~ "T Stage",
      grade ~ "Tumor Grade",
      response ~ "Tumor Response",
      death ~ "Patient Died"
    )
  ) |>
  modify_header(label = "") |>
  bold_labels() |>
  add_p() |>
  bold_p() |>
  add_overall() |>
  clean_table() |>
  add_variable_group_header(
    header = "Demographics",
    variables = age:stage
  ) |>
  add_variable_group_header(
    header = "Clinical Measures",
    variables = grade:ttdeath
  ) |>
  add_group_styling() |>
  add_group_colors(
    color = c("#E1EAE2", "#FFF9E6")
  )
```
:::

::: {.column}

```{r}
#| eval: false
dat |>
  tbl_summary(by = trt) |>
  extras() |>
  add_variable_group_header(
    header = "Demographics",
    variables = age:stage
  ) |>
  add_variable_group_header(
    header = "Clinical Measures",
    variables = grade:ttdeath
  ) |>
  add_group_styling() |>
  add_group_colors(
    color = c("#E1EAE2", "#FFF9E6")
  )
```
::: {.fragment}
We just cut our code **in half**... 
and that was only one table!
:::

:::

::::




# Add slide about:

Removing or turning off parameters in `extras()`!!!





## `sumExtras` supports most `gtsummary` table types

with either partial or full features available.

* `tbl_summary()`
* `tbl_svysummary()`
* `tbl_regression()`
* `tbl_uvregression()`
* `tbl_strata()`
* `tbl_merge()`
* `tbl_cross()`

## THANK YOU! 😄

:::{.absolute bottom="30%" left="-8%"}
<span style="font-size: 50px;"><strong>Kyle Grealis, MS</strong></span>

**GitHub**: https://github.com/kyleGrealis

**Email**: kyleGrealis@proton.me

**Slides**: https://slides.kyleGrealis.com/sumExtras

**Package**: https://www.kyleGrealis.com/sumExtras
:::

![](img/kyle.jpg){.styled-image .absolute bottom="30%" right="1%" height="auto" width="190"}

